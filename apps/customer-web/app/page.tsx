import { Button } from '@mui/material';

export default async function Index() {
  return (
    <Button
      variant="contained"
      size="medium"
      fullWidth
      color="primary"
      // disabled
      // startIcon={'s'}
    >
      Hello
    </Button>
  );
}
